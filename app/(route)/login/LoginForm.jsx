import {LoginForm} from "./LoginForm"

export const metadata = {
  title: "Login - CORPORATIVO PAGUE MENOS",
  description: "Faça login no sistema",
};

export default function LoginPage() {
  return <LoginForm />;
}
